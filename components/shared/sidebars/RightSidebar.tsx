import React, { FunctionComponent } from "react";

interface OwnProps {}

type Props = OwnProps;

const RightSidebar: FunctionComponent<Props> = (props) => {
  return <div>Right Side bar</div>;
};

export default RightSidebar;

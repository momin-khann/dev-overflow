import React, { FunctionComponent } from "react";

interface OwnProps {}

type Props = OwnProps;

const page: FunctionComponent<Props> = (props) => {
  return <div>Question Details Page with Dynamic Id</div>;
};

export default page;
